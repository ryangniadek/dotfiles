" Disable compatibility with vi
set nocompatible

" Encoding
set encoding=utf-8

" Enable file type detection, plugins and indentations
filetype on
filetype plugin on
filetype indent on

" Syntax highlighting
syntax on

" Line Numbers
set number
set relativenumber

" Show file stats
set ruler

" Blink cursor on error instead of beep
set visualbell

" Set shift and tab width
set shiftwidth=4
set tabstop=4
set expandtab

